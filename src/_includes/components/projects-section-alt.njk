<section id="projects">
    {% for section in site.workshopIntro %}
    <div class="project-category project-category--intro-{{ loop.index }}">
        <h2>{{ section.title }}</h2>
        <p class="about-bio">{{ section.text }}</p>
    </div>
    {% endfor %}
    
    {% include "components/author-name.njk" %}
</section>
